const GridItem = defineComponent(() => {
  void 0;
  return () => (
    <div class="grid-item transition duration-200" />
  );
});

const GridLayout = defineComponent({

});

GridLayout.Item = GridItem;

export default GridLayout;
